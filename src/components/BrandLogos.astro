---
import { getCollection } from 'astro:content';

const allVehicles = await getCollection('vehicles');

// Count vehicles per make
const makeCounts = new Map<string, number>();
allVehicles.forEach(v => {
  makeCounts.set(v.data.make, (makeCounts.get(v.data.make) || 0) + 1);
});

const brands = Array.from(makeCounts.entries())
  .sort((a, b) => b[1] - a[1])
  .map(([name, count]) => ({ name, count }));

// Body types with car images from theme
const bodyTypes = [
  { name: 'Coupe', image: '/assets/images/icon-box/car-list1.png' },
  { name: 'SUV', image: '/assets/images/icon-box/car-list2.png' },
  { name: 'Sedan', image: '/assets/images/icon-box/car-list3.png' },
  { name: 'Hatchback', image: '/assets/images/icon-box/car-list4.png' },
  { name: 'Pickup Truck', image: '/assets/images/icon-box/car-list5.png' },
  { name: 'Crossover', image: '/assets/images/icon-box/car-list6.png' },
];

// Count vehicles per body type
const bodyCounts = new Map<string, number>();
allVehicles.forEach(v => {
  bodyCounts.set(v.data.bodyType, (bodyCounts.get(v.data.bodyType) || 0) + 1);
});
---

<section class="py-14 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header with tabs -->
    <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-10">
      <h2 class="text-2xl lg:text-3xl font-bold text-gray-900 font-[family-name:var(--font-heading)]">Popular Car Makes & Body Types</h2>
      <div class="flex items-center gap-4 mt-4 sm:mt-0">
        <button class="brand-tab active text-sm font-medium text-[var(--color-primary)] border-b-2 border-[var(--color-primary)] pb-1 transition-all" data-tab="body-types">Car body types</button>
        <button class="brand-tab text-sm font-medium text-gray-400 hover:text-gray-600 pb-1 border-b-2 border-transparent transition-all" data-tab="car-makes">Car makes</button>
        <a href="/vehicles" class="text-sm font-medium text-gray-400 hover:text-[var(--color-primary)] transition-colors">View all →</a>
      </div>
    </div>

    <!-- Body Types Tab (default) -->
    <div id="body-types-panel" class="brand-panel">
      <div class="swiper body-type-swiper">
        <div class="swiper-wrapper">
          {bodyTypes.map(bt => (
            <div class="swiper-slide">
              <a href={`/vehicles?bodyType=${bt.name}`} class="flex flex-col items-center p-4 rounded-xl hover:shadow-md transition-all group text-center">
                <div class="w-full aspect-[4/3] mb-3 flex items-center justify-center">
                  <img src={bt.image} alt={bt.name} class="max-w-full max-h-full object-contain group-hover:scale-105 transition-transform duration-300" loading="lazy" />
                </div>
                <span class="text-sm font-semibold text-gray-800 font-[family-name:var(--font-heading)]">{bt.name}</span>
                <span class="text-xs text-gray-400 mt-0.5">{bodyCounts.get(bt.name) || 0} Car{(bodyCounts.get(bt.name) || 0) !== 1 ? 's' : ''}</span>
              </a>
            </div>
          ))}
        </div>
        <div class="swiper-pagination body-type-pagination mt-6"></div>
      </div>
    </div>

    <!-- Car Makes Tab -->
    <div id="car-makes-panel" class="brand-panel hidden">
      <div class="swiper brand-swiper">
        <div class="swiper-wrapper">
          {brands.map(brand => (
            <div class="swiper-slide">
              <a href={`/vehicles?make=${brand.name}`} class="flex flex-col items-center p-5 rounded-xl border border-gray-100 hover:border-[var(--color-primary)] hover:shadow-md transition-all group">
                <div class="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-3">
                  <span class="text-lg font-bold text-gray-700 group-hover:text-[var(--color-primary)] transition-colors font-[family-name:var(--font-heading)]">
                    {brand.name}
                  </span>
                </div>
                <span class="text-xs text-gray-400 mt-1">{brand.count} Car{brand.count !== 1 ? 's' : ''}</span>
              </a>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import Swiper from 'swiper';
  import { Autoplay, Pagination } from 'swiper/modules';
  import 'swiper/css';
  import 'swiper/css/pagination';

  new Swiper('.body-type-swiper', {
    modules: [Autoplay, Pagination],
    slidesPerView: 2,
    spaceBetween: 16,
    loop: true,
    autoplay: { delay: 4000, disableOnInteraction: false },
    pagination: { el: '.body-type-pagination', clickable: true },
    breakpoints: {
      480: { slidesPerView: 3 },
      768: { slidesPerView: 4 },
      1024: { slidesPerView: 5 },
      1280: { slidesPerView: 6 },
    },
  });

  new Swiper('.brand-swiper', {
    modules: [Autoplay],
    slidesPerView: 2,
    spaceBetween: 16,
    loop: true,
    autoplay: { delay: 3000, disableOnInteraction: false },
    breakpoints: {
      480: { slidesPerView: 3 },
      640: { slidesPerView: 4 },
      768: { slidesPerView: 5 },
      1024: { slidesPerView: 6 },
      1280: { slidesPerView: 7 },
    },
  });

  // Tab switching
  const tabs = document.querySelectorAll<HTMLButtonElement>('.brand-tab');
  const panels = document.querySelectorAll<HTMLElement>('.brand-panel');

  tabs.forEach(tab => {
    tab.addEventListener('click', () => {
      tabs.forEach(t => {
        t.classList.remove('active', 'text-[var(--color-primary)]', 'border-[var(--color-primary)]');
        t.classList.add('text-gray-400', 'border-transparent');
      });
      tab.classList.add('active', 'text-[var(--color-primary)]', 'border-[var(--color-primary)]');
      tab.classList.remove('text-gray-400', 'border-transparent');

      panels.forEach(p => p.classList.add('hidden'));
      const target = tab.dataset.tab;
      document.getElementById(`${target}-panel`)?.classList.remove('hidden');
    });
  });
</script>
